## Bootstrap config for hdp-product-service. Full config in
## github.com/your-org/ecommerce-config-repo → hdp-product-service.yml

spring:
  application:
    name: hdp-product-service

  config:
    import: ${SPRING_CONFIG_IMPORT:configserver:http://localhost:8888}

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:default}
